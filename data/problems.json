[
  {
    "id": "two-sum",
    "title": "Two Sum",
    "difficulty": "easy",
    "topics": ["arrays", "hashmap"],
    "prompt": "Given an array of integers nums and an integer target, return indices of the two numbers that add up to target. Assume exactly one solution and you may not use the same element twice.",
    "starter_code": "def two_sum(nums, target):\n    # return [i, j]\n    pass\n\n# Driver (do not change)\nimport ast, sys\nline = sys.stdin.read().strip().split('\\n')\nnums = ast.literal_eval(line[0])\ntarget = int(line[1])\nprint(two_sum(nums, target))\n",
    "sample_tests": [
      {"stdin": "[2, 7, 11, 15]\n9", "expected": "[0, 1]"},
      {"stdin": "[3, 2, 4]\n6", "expected": "[1, 2]"}
    ],
    "hidden_tests": [
      {"stdin": "[3, 3]\n6", "expected": "[0, 1]"},
      {"stdin": "[1, 5, 3, 7]\n8", "expected": "[0, 3]"}
    ],
    "good_ideas": [
      "hash map of value to index while scanning once",
      "for each number check if target-num already seen",
      "O(n) time single pass dictionary"
    ],
    "weak_ideas": ["brute force nested loops only", "sort then two pointer without restoring indices"]
  },
  {
    "id": "valid-parentheses",
    "title": "Valid Parentheses",
    "difficulty": "easy",
    "topics": ["stack", "strings"],
    "prompt": "Given a string s containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid. Open brackets must be closed by the same type in the correct order.",
    "starter_code": "def is_valid(s):\n    # return True or False\n    pass\n\nimport sys\ns = sys.stdin.read().strip()\nprint(str(is_valid(s)))\n",
    "sample_tests": [
      {"stdin": "()", "expected": "True"},
      {"stdin": "()[]{}", "expected": "True"},
      {"stdin": "(]", "expected": "False"}
    ],
    "hidden_tests": [
      {"stdin": "([{}])", "expected": "True"},
      {"stdin": "(((", "expected": "False"}
    ],
    "good_ideas": ["stack push opens pop on matching close", "map closing to opening", "empty stack at end"],
    "weak_ideas": ["count brackets only", "regex replace pairs repeatedly without stack explanation"]
  },
  {
    "id": "reverse-linked-list-concept",
    "title": "Reverse a Linked List (array simulation)",
    "difficulty": "easy",
    "topics": ["linked-list", "pointers"],
    "prompt": "You are given a list of node values representing a singly linked list. Reverse the list and return the values in reversed order. Implement iteratively with prev/curr pointers (simulate nodes as needed, or reverse in-place on a list while explaining pointer logic).",
    "starter_code": "def reverse_list(values):\n    # return reversed values\n    pass\n\nimport ast, sys\nvalues = ast.literal_eval(sys.stdin.read().strip())\nprint(reverse_list(values))\n",
    "sample_tests": [
      {"stdin": "[1, 2, 3, 4, 5]", "expected": "[5, 4, 3, 2, 1]"},
      {"stdin": "[1, 2]", "expected": "[2, 1]"}
    ],
    "hidden_tests": [
      {"stdin": "[]", "expected": "[]"},
      {"stdin": "[42]", "expected": "[42]"}
    ],
    "good_ideas": ["three pointers prev curr next", "iterative reverse O(1) extra space", "walk list rewiring next"],
    "weak_ideas": ["only use python slice [::-1] with no pointer discussion"]
  },
  {
    "id": "max-subarray",
    "title": "Maximum Subarray Sum",
    "difficulty": "medium",
    "topics": ["arrays", "dp", "kadane"],
    "prompt": "Given an integer array nums, find the contiguous subarray with the largest sum and return that sum.",
    "starter_code": "def max_sub_array(nums):\n    # return max sum\n    pass\n\nimport ast, sys\nnums = ast.literal_eval(sys.stdin.read().strip())\nprint(max_sub_array(nums))\n",
    "sample_tests": [
      {"stdin": "[-2,1,-3,4,-1,2,1,-5,4]", "expected": "6"},
      {"stdin": "[1]", "expected": "1"}
    ],
    "hidden_tests": [
      {"stdin": "[5,4,-1,7,8]", "expected": "23"},
      {"stdin": "[-1,-2,-3]", "expected": "-1"}
    ],
    "good_ideas": ["kadane keep running best", "reset running sum when negative", "O(n) single pass"],
    "weak_ideas": ["only O(n^2) nested without discussing better"]
  },
  {
    "id": "binary-search",
    "title": "Binary Search",
    "difficulty": "easy",
    "topics": ["binary-search", "arrays"],
    "prompt": "Given a sorted array of distinct integers and a target, return the index if found else -1. Must be O(log n).",
    "starter_code": "def search(nums, target):\n    # return index or -1\n    pass\n\nimport ast, sys\nline = sys.stdin.read().strip().split('\\n')\nnums = ast.literal_eval(line[0])\ntarget = int(line[1])\nprint(search(nums, target))\n",
    "sample_tests": [
      {"stdin": "[-1,0,3,5,9,12]\n9", "expected": "4"},
      {"stdin": "[-1,0,3,5,9,12]\n2", "expected": "-1"}
    ],
    "hidden_tests": [
      {"stdin": "[5]\n5", "expected": "0"},
      {"stdin": "[1,2,3,4,5]\n1", "expected": "0"}
    ],
    "good_ideas": ["low high mid loop", "halve search space", "avoid mid overflow style"],
    "weak_ideas": ["linear scan", "recursion without bounds"]
  },
  {
    "id": "merge-intervals-lite",
    "title": "Merge Overlapping Intervals",
    "difficulty": "medium",
    "topics": ["arrays", "sorting", "intervals"],
    "prompt": "Given a list of intervals [start, end], merge all overlapping intervals and return the merged list sorted by start.",
    "starter_code": "def merge(intervals):\n    # return merged intervals\n    pass\n\nimport ast, sys\nintervals = ast.literal_eval(sys.stdin.read().strip())\nprint(merge(intervals))\n",
    "sample_tests": [
      {"stdin": "[[1,3],[2,6],[8,10],[15,18]]", "expected": "[[1, 6], [8, 10], [15, 18]]"},
      {"stdin": "[[1,4],[4,5]]", "expected": "[[1, 5]]"}
    ],
    "hidden_tests": [
      {"stdin": "[[1,4],[0,4]]", "expected": "[[0, 4]]"},
      {"stdin": "[[1,4],[2,3]]", "expected": "[[1, 4]]"}
    ],
    "good_ideas": ["sort by start then merge if overlap", "track current end", "append when gap"],
    "weak_ideas": ["compare every pair without sorting"]
  }
]
